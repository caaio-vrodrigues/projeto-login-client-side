// next.config.js

// Remova a linha "import type { NextConfig } from "next";"
// Remova a anotação de tipo ": NextConfig"

const nextConfig = {
  /* config options here */
  // Exemplo: se você tinha alguma configuração, ela vai aqui.
  // Por exemplo, para Next.js 14, se você usasse o App Router:
  // experimental: {
  //   appDir: true,
  // },
};

export default nextConfig;
